import 'package:flutter/material.dart';

abstract class Styling {
  static const textColour = Color.fromRGBO(50, 50, 93, 20);
  static const iconColour = Color.fromRGBO(216, 79, 153, 3);
  static const mainBackgroundColour = Color.fromRGBO(247, 247, 250, 10);
  static const mainBackgroundColourTest = Color.fromRGBO(240, 240, 250, 100);
  static const navBarBackgroundColour = Color.fromRGBO(255, 255, 255, 100);
  static const widgetBackgroundColour = Color.fromRGBO(247, 247, 250, 10);
}
